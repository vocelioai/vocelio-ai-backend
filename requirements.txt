# requirements.txt - Fixed with all necessary dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
httpx==0.25.2
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6

# These are included in uvicorn[standard] but let's be explicit
httptools==0.6.1
uvloop==0.19.0
python-dotenv==1.0.0

# Optional: For future database needs
# asyncpg==0.29.0
# redis==5.0.1
