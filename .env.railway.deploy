# Vocelio Backend Environment Configuration

# Database Configuration
DATABASE_URL=postgresql://${{PGUSER}}:${{PGPASSWORD}}@${{PGHOST}}:${{PGPORT}}/${{PGDATABASE}}
SUPABASE_URL=https://oguudxyffjjasilhqixc.supabase.co
SUPABASE_KEY=your_supabase_anon_key_here

# Redis Configuration
REDIS_URL=${{REDIS_URL}}

# Authentication
JWT_SECRET_KEY=your_jwt_secret_key_here
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# External API Keys
OPENAI_API_KEY=your_openai_api_key_here
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here
RAMBLE_API_KEY=your_ramble_api_key_here

# Twilio Configuration
TWILIO_ACCOUNT_SID=your_twilio_account_sid_here
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here

# Stripe Configuration
STRIPE_SECRET_KEY=your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret_here
STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key_here

# Service Configuration
ENVIRONMENT=production
DEBUG=false
CORS_ORIGINS=https://your-frontend-domain.com

# Service Ports (for load balancing)
OVERVIEW_HOST=localhost
OVERVIEW_PORT=8001
AGENTS_HOST=localhost
AGENTS_PORT=8002
SMART_CAMPAIGNS_HOST=localhost
SMART_CAMPAIGNS_PORT=8003
CALL_CENTER_HOST=localhost
CALL_CENTER_PORT=8004
PHONE_NUMBERS_HOST=localhost
PHONE_NUMBERS_PORT=8005
VOICE_MARKETPLACE_HOST=localhost
VOICE_MARKETPLACE_PORT=8006
VOICE_LAB_HOST=localhost
VOICE_LAB_PORT=8007
FLOW_BUILDER_HOST=localhost
FLOW_BUILDER_PORT=8008
ANALYTICS_PRO_HOST=localhost
ANALYTICS_PRO_PORT=8009
AI_BRAIN_HOST=localhost
AI_BRAIN_PORT=8010
INTEGRATIONS_HOST=localhost
INTEGRATIONS_PORT=8011
AGENT_STORE_HOST=localhost
AGENT_STORE_PORT=8012
BILLING_PRO_HOST=localhost
BILLING_PRO_PORT=8013
TEAM_HUB_HOST=localhost
TEAM_HUB_PORT=8014
COMPLIANCE_HOST=localhost
COMPLIANCE_PORT=8015
WHITE_LABEL_HOST=localhost
WHITE_LABEL_PORT=8016
DEVELOPER_API_HOST=localhost
DEVELOPER_API_PORT=8017
SETTINGS_HOST=localhost
SETTINGS_PORT=8018
SCRIPTS_HOST=localhost
SCRIPTS_PORT=8019
NOTIFICATIONS_HOST=localhost
NOTIFICATIONS_PORT=8020
WEBHOOKS_HOST=localhost
WEBHOOKS_PORT=8021

# Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60

# Logging
LOG_LEVEL=info
LOG_FORMAT=json

# Voice Provider Configuration
PIPER_TTS_MODEL_PATH=./models/piper
VOICE_CLONE_STORAGE_PATH=./storage/voice-clones

# File Upload Configuration
MAX_FILE_SIZE=50000000  # 50MB
ALLOWED_AUDIO_FORMATS=wav,mp3,ogg,flac
ALLOWED_IMAGE_FORMATS=jpg,jpeg,png,gif

# Webhook Configuration
WEBHOOK_SECRET=your_webhook_secret_here
WEBHOOK_TIMEOUT=30

# Monitoring
SENTRY_DSN=your_sentry_dsn_here
PROMETHEUS_ENABLED=true
PROMETHEUS_PORT=9090

# Deployment Mode
NODE_ENV=production

# API Gateway
API_GATEWAY_HOST=0.0.0.0
API_GATEWAY_PORT=8000

# Email Notifications
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_pass
DEFAULT_FROM_EMAIL=support@vocelio.ai

# AI Fallback
OPENAI_MODEL=gpt-4o-mini